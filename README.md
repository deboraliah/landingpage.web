# 🌐 Projeto – Landing Page

 | VIzualizar Projeto|Link |
 | ------------- | ---------------------------------------------------------------------------------------------------------------- |
 |Landing Page                |[Projeto](https://deboraliah.github.io/landingpage.web/)


## 📖 Sobre o Projeto
Este projeto foi desenvolvido como parte da disciplina **Desenvolvimento Web I** do **Instituto Federal do Ceará – Campus Maranguape**, ministrada pelo professor **Thomaz Maia**.

O objetivo é criar uma **landing page** para promover um **servidor do IFCE**, aplicando os conceitos fundamentais de **HTML** e **CSS** aprendidos em aula.  
A proposta consiste em desenvolver um site simples, estático e sem o uso de *frameworks* ou técnicas avançadas de layout.

---

## 🎯 Objetivo do Trabalho
O site deve apresentar informações sobre um servidor (professor, técnico administrativo, intérprete de Libras etc.) e conter:
- Uma **página principal (index.html)** com foto, nome e biografia;
- **Subpáginas** com informações adicionais (formação, disciplinas, contato, etc.);
- Um **formulário de contato funcional (HTML)**;
- Um **menu de navegação** que interliga todas as páginas do site;
- Um **link para download do currículo em PDF**.

---

## 🧩 Estrutura das Páginas

### 🏠 Página Principal – `index.html`
Deve conter:
- Nome completo do servidor em destaque (`<h1>`);
- Foto (`<img>`);
- Pequena biografia (`<p>`);
- Menu de navegação (`<nav>` e `<a>`);
- Link/botão para baixar o currículo (PDF);
- Uso de tags semânticas como `header`, `section`, `article`, `aside`, `footer`.

---

## 🎨 Regras de Estilo (CSS)

Todo o estilo está centralizado em um **único arquivo externo**:  
`css/style.css`

Devem ser aplicados:
- Cores personalizadas (fundo, texto, links, etc.);
- Estilização de fontes (tamanho, cor, tipo);
- Box model: `padding`, `margin` e `border`;
- Posicionamento com `position: relative` e `absolute` quando necessário;
- Estilização de tabelas, listas, links e formulários.

> ⚠️ **Atenção:**  
> É **proibido** o uso de **Flexbox (`display: flex`)** ou **Grid Layout (`display: grid`)** neste projeto.

---

## 💻 Tecnologias Utilizadas
- **HTML5**
- **CSS3**
- Editor: *Visual Studio Code*

